# Your personal MongoDB connection string.
# Fill this in your private .env.local file.
MONGODB_URI=""

# The default database name.
# You can usually leave this as-is unless you changed the sample name on MongoDB Atlas/Compass.
DB_NAME="sample_airbnb"

# A long, random string used for signing authentication tokens (JWTs).
# Generate one for your .env.local file by running: node -e "console.log(require('crypto').randomBytes(32).toString('base64'));"
JWT_SECRET=""